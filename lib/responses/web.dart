import 'package:flutter/material.dart';

class web extends StatelessWidget {
  const web({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text("Welcome to web")),
    ) ;
  }
}